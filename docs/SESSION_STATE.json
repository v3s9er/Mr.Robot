{
  "schema": "mr-robot-session-state/v11",
  "updatedAt": "2026-09-06",
  "project": "Mr.Robot repository root",
  "release": {
    "version": "0.4.7",
    "releaseRef": "v0.4.7",
    "gitBranch": "main",
    "state": "local desktop and Discord workspace deployed and verified; GitHub publication follows this commit"
  },
  "resumeObjective": "Use ai_talk ticket panel; verify v0.4.7 GitHub assets if resuming. Never export runtime IDs, bot credentials or thread mappings.",
  "changes": [
    "Per-question economy64k/standard256k/quality1M/adaptive/unlimited budgets with fresh allowance each question.",
    "Desktop and mobile composer budget selectors; chat.start no generic RPC deadline.",
    "Registered Discord server administrators only; per-user /robot access with explicit full confirmation, result and approval retrieval.",
    "Discord host-scoped full override cannot be forged by remote JSON and cannot bypass global read-only; Discord uses audit-only token policy.",
    "Discord ai_talk ticket panel: explicit title modal creates private user-owned thread; persistent panel/list/settings/stop/archive/confirmed delete.",
    "Plain messages from session owner run through existing administrator-checked agent path; bounded per-thread queue and cancellation race guard.",
    "PC administrator can provision ai_talk with admin-only visibility; no user tickets created automatically."
  ],
  "status": {
    "typecheck": "Shared, Agent, Web, Mobile passed",
    "tests": "19 Python authorization/ticket tests; host session/Discord authority/integration/question-budget tests; Shared/Agent/Web/Mobile typechecks; core hardening, UI contracts and calendar release-version tests passed. Public release hygiene passed.",
    "discord": "Gateway ready and idle; administrator-only bind/unbind/sessions commands verified via Discord API. Message Content enabled. ai_talk has administrator-only visibility with pinned ticket/list panel. Private runtime IDs omitted. No paid model task submitted.",
    "desktopInstall": "0.4.7.0 installed and launched; installer retry exit0; installed app.asar matches final build and both Python modules match source.",
    "distribution": "Windows EXE and source intended for GitHub v0.4.7; no new APK. Read GitHub release for publication confirmation."
  },
  "artifacts": {
    "installer": "release/Mr.Robot-Setup-0.4.7-x64.exe",
    "android": "Mobile source budget UI updated; no new APK built",
    "source": "release/Mr.Robot-source-0.4.7.zip"
  },
  "security": {
    "discord": "No public port. Device credential retained by Node host only. Registered server administrator role revalidated per request and during runs. Per-user approvals/results. PC global read-only remains authoritative.",
    "publication": "Only generic adapter source is bundled. Existing bot token, config, logs, databases and runtime credentials excluded."
  }
}
